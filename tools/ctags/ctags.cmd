@echo off
rem Minimal ctags shim for Arduino CLI when the builtin Arduino ctags package
rem is unavailable. This sketch declares functions before use, so empty tags
rem are enough for the Arduino preprocessor.
exit /b 0
